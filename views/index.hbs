<!-- search bar -->
<div class="container search-bar">
  <div class="search-bar-outer">
    <div class="search-bar-inner">
      <form action="/search">
        <h4 class="mb-2">尋找餐廳：</h4>
        <div class="input-group">
          {{#if restaurantList.length}}
          <input type="text" name="keyword" class="form-control" placeholder="輸入餐廳、分類，多筆輸入請用,分隔"
            aria-label="Restaurant Name..." value="{{ keyword }}" aria-describedby="search-button">
          <div class="input-group-append">
            <button class="btn btn-major" type="submit" id="search-button"><i class="fas fa-search"></i></button>
          </div>
        </div>

        {{else}}
        <input type="text" name="keyword" class="form-control" placeholder="輸入餐廳、分類，多筆輸入請用,分隔"
          aria-label="Restaurant Name..." value="{{ keyword }}" aria-describedby="search-button">
        <div class="input-group-append">
          <button class="btn btn-major" type="submit" id="search-button"><i class="fas fa-search"></i></button>
        </div>
    </div>
    <div>您搜尋的關鍵字<b>{{ keyword }}</b>不存在，請重新搜尋</div>
    {{/if}}
    </form>
  </div>
</div>
</div>
<div id="add-page-scroll">
  <a href="./restaurants/new">
    <i class="fa-duotone fa-solid fa-circle-plus fa-2xl"></i>
  </a>
</div>

<!-- restaurant list -->

<div class="container mt-5">
  <div class="row">

    <div class="col-md-10 col-12 mx-auto">

      <div class="card-columns">
        {{#each restaurantList}}

        <a href="./restaurants/{{this._id}}" class="text-secondary" style="text-decoration:none;">
          <div class="card mb-3">
            <img class="card-img-top" src="{{this.image}}" alt="{{this.name}}">
            <div class="card-body p-3">
              <h6 class="card-title mb-1">{{this.name}}</h6>

              <div class="restaurant-category mb-1">
                <i class="fas fa-utensils pr-2"></i> {{this.category}}
              </div>

              <div class="d-flex justify-content-between align-items-center mr-1">
                <span class="badge badge-pill badge-danger font-weight-normal">
                  {{this.rating}}
                  <i class="fas fa-star fa-xs"></i>
                </span>
                <div class="d-flex justify-content-end align-items-center mr-1">
                  <a href="./restaurants/{{this._id}}/edit" class="btn btn-link p-2">Edit</a>
                  <form action="./restaurants/{{this._id}}/delete" method="POST" style="display: inline;">
                    <button type="submit" class="btn btn-link link-danger p-2">Delete</button>
                  </form>
                </div>
              </div>
            </div>
          </div>
        </a>
        {{/each}}
      </div>
    </div>
  </div>
</div>